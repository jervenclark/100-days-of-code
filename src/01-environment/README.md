# Environment

## Prerequisites

Be sure to have docker engine and docker compose in your host system. Also, check that the docker daemon is running and can be accessed by your user.

## Building Services

Simply `docker-compose build` to build your container.
